{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Extension",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/*.js"
      ],
      "request": "launch",
      "type": "extensionHost"
    }
  ]
}
